lib_component_agent_append_event_to_pattern
=> nrel_main_idtf:
	[Компонент библиотеки. sc-агент добавления события в шаблон поиска]
		(* <- lang_ru;; *);

<- library_of_platform_dependent_reusable_components; 
<- library_of_atomic_reusable_components;

-> rrel_key_sc_element: 
	.c_realization_of_append_event_to_pattern;

<- key_sc_element:
	...
	(*
	<- explanation;;
	<= nrel_sc_text_translation:
		...
		(*
		-> rrel_example:
			[Задачей sc-агента добавления события в шаблон поиска является добавление в заданный шаблон поиска книги сведений об одном из событий её сюжета];;
		*);;
	*);;

question_append_event_to_pattern
<- sc_node_not_relation;
=> nrel_main_idtf:
 	[действие. добавить событие в шаблон поиска]
		(* <- lang_ru;; *);
	[action. append event to search pattern]
		(* <- lang_en;;	*);;
